{
  "name": "@hangar/shared",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "scripts": {
    "build": "tsc && cp src/default-config.json dist/ && cp -r src/dockerfiles dist/",
    "clean": "rm -rf dist"
  },
  "devDependencies": {
    "@types/bun": "^1.3.6",
    "drizzle-kit": "^0.31.8",
    "typescript": "^5.7.3"
  },
  "dependencies": {
    "@libsql/client": "^0.17.0",
    "drizzle-orm": "^0.45.1",
    "e2b": "^2.3.0",
    "zod": "^4.3.5"
  }
}
